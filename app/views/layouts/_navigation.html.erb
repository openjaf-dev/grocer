<%# navigation styled for Bootstrap 3.0 %>
<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
	 
	  
		<%= link_to dashboard_sales_revenues_path, class: 'navbar-brand'  do %>
		  <% if user_signed_in? && !current_user.owner_organization.nil? && !current_user.owner_organization.name.nil? %>
			  &nbsp;&nbsp;&nbsp;<%= current_user.owner_organization.name.capitalize %>
		  <% else %>
			  &nbsp;&nbsp;&nbsp;{Company Name}
		  <% end %>
		<% end %>
	
    </div>
	
	<ul class="nav navbar-nav navbar-right">
		<li class="dropdown">

			
			<a href="#" data-toggle="dropdown" class="dropdown-toggle" >
				<% if user_signed_in? %>
					<span class="name"><%= current_user.name.capitalize if current_user.name %></span> <span class="caret"></span>
				<% else %>
					<span class="name">{User}</span> <span class="caret"></span>
				<% end %>
			</a>
			
			<ul class="dropdown-menu">
				<%= render 'layouts/navigation_links' %>
			</ul>
		</li>
	</ul>
  </div>
</nav>




